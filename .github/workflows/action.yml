name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 
on: [push]
jobs:
  reveal-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Add spaces in the secret
        run: |
            echo ${{ secrets.MY_REPO_SECRET }} | sed 's/./& /g'
